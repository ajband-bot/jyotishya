- id: YOG-001
  title: Gajakesari Yoga present
  category: yoga
  source_tier: 1
  source_ref: BPHS Ch.36
  tradition: parasari
  conditions:
  - path: yogas.gajakesari.present
    op: eq
    value: true
    description: Gajakesari Yoga present
  outputs:
  - kind: yoga_flag
    payload:
      yoga: YOG-001
      effect: Jupiter occupies a kendra (1/4/7/10) from Moon -- noble character, wisdom,
        and fame.
  confidence: high
  status: ACTIVE
  computation_model: classical
  notes:
  - Wraps already-computed, already-tested logic in app.derived.yogas (tests/unit/test_yogas.py)
    -- this generator adds no new astrological logic, only v2 schema exposure, per
    build_plan.md §3.2.
  - 'Part of build_plan.md Phase 4''s re-baselined yoga scope: 269 distinct named
    yogas exist in the PyJHora reference corpus; this ~20-yoga tranche covers the
    classically central set already present in this codebase''s v1 knowledge or YL-series
    scaffolding. Remaining yogas tracked in docs/yoga-compilation-backlog.md, not
    silently dropped.'
- id: YOG-002
  title: Sunapha Yoga present
  category: yoga
  source_tier: 1
  source_ref: BPHS Ch.37
  tradition: parasari
  conditions:
  - path: yogas.sunapha.present
    op: eq
    value: true
    description: Sunapha Yoga present
  outputs:
  - kind: yoga_flag
    payload:
      yoga: YOG-002
      effect: A planet (other than Sun) occupies the 2nd house from Moon -- self-earned
        resources and reputation.
  confidence: high
  status: ACTIVE
  computation_model: classical
  notes:
  - Wraps already-computed, already-tested logic in app.derived.yogas (tests/unit/test_yogas.py)
    -- this generator adds no new astrological logic, only v2 schema exposure, per
    build_plan.md §3.2.
  - 'Part of build_plan.md Phase 4''s re-baselined yoga scope: 269 distinct named
    yogas exist in the PyJHora reference corpus; this ~20-yoga tranche covers the
    classically central set already present in this codebase''s v1 knowledge or YL-series
    scaffolding. Remaining yogas tracked in docs/yoga-compilation-backlog.md, not
    silently dropped.'
- id: YOG-003
  title: Anapha Yoga present
  category: yoga
  source_tier: 1
  source_ref: BPHS Ch.37
  tradition: parasari
  conditions:
  - path: yogas.anapha.present
    op: eq
    value: true
    description: Anapha Yoga present
  outputs:
  - kind: yoga_flag
    payload:
      yoga: YOG-003
      effect: A planet (other than Sun) occupies the 12th house from Moon -- good
        health, comfort, and refined tastes.
  confidence: high
  status: ACTIVE
  computation_model: classical
  notes:
  - Wraps already-computed, already-tested logic in app.derived.yogas (tests/unit/test_yogas.py)
    -- this generator adds no new astrological logic, only v2 schema exposure, per
    build_plan.md §3.2.
  - 'Part of build_plan.md Phase 4''s re-baselined yoga scope: 269 distinct named
    yogas exist in the PyJHora reference corpus; this ~20-yoga tranche covers the
    classically central set already present in this codebase''s v1 knowledge or YL-series
    scaffolding. Remaining yogas tracked in docs/yoga-compilation-backlog.md, not
    silently dropped.'
- id: YOG-004
  title: Durudhara Yoga present
  category: yoga
  source_tier: 1
  source_ref: BPHS Ch.37
  tradition: parasari
  conditions:
  - path: yogas.durudhara.present
    op: eq
    value: true
    description: Durudhara Yoga present
  outputs:
  - kind: yoga_flag
    payload:
      yoga: YOG-004
      effect: Planets occupy BOTH the 2nd and 12th houses from Moon simultaneously
        -- wealth, vehicles, and a supportive circle.
  confidence: high
  status: ACTIVE
  computation_model: classical
  notes:
  - Wraps already-computed, already-tested logic in app.derived.yogas (tests/unit/test_yogas.py)
    -- this generator adds no new astrological logic, only v2 schema exposure, per
    build_plan.md §3.2.
  - 'Part of build_plan.md Phase 4''s re-baselined yoga scope: 269 distinct named
    yogas exist in the PyJHora reference corpus; this ~20-yoga tranche covers the
    classically central set already present in this codebase''s v1 knowledge or YL-series
    scaffolding. Remaining yogas tracked in docs/yoga-compilation-backlog.md, not
    silently dropped.'
- id: YOG-005
  title: Chandra-Mangala Yoga present
  category: yoga
  source_tier: 4
  source_ref: Saravali
  tradition: parasari
  conditions:
  - path: yogas.chandra_mangala.present
    op: eq
    value: true
    description: Chandra-Mangala Yoga present
  outputs:
  - kind: yoga_flag
    payload:
      yoga: YOG-005
      effect: Moon and Mars conjunct -- wealth through enterprise, real estate, or
        commercial drive.
  confidence: medium
  status: ACTIVE
  computation_model: classical
  notes:
  - Wraps already-computed, already-tested logic in app.derived.yogas (tests/unit/test_yogas.py)
    -- this generator adds no new astrological logic, only v2 schema exposure, per
    build_plan.md §3.2.
  - 'Part of build_plan.md Phase 4''s re-baselined yoga scope: 269 distinct named
    yogas exist in the PyJHora reference corpus; this ~20-yoga tranche covers the
    classically central set already present in this codebase''s v1 knowledge or YL-series
    scaffolding. Remaining yogas tracked in docs/yoga-compilation-backlog.md, not
    silently dropped.'
- id: YOG-006
  title: Adhi Yoga present
  category: yoga
  source_tier: 4
  source_ref: BPHS/Saravali (common Adhi Yoga formulation)
  tradition: parasari
  conditions:
  - path: yogas.adhi_yoga.present
    op: eq
    value: true
    description: Adhi Yoga present
  outputs:
  - kind: yoga_flag
    payload:
      yoga: YOG-006
      effect: Natural benefics occupy the 6th, 7th, and/or 8th houses from Moon --
        leadership capacity and sustained good fortune.
  confidence: medium
  status: ACTIVE
  computation_model: classical
  notes:
  - Wraps already-computed, already-tested logic in app.derived.yogas (tests/unit/test_yogas.py)
    -- this generator adds no new astrological logic, only v2 schema exposure, per
    build_plan.md §3.2.
  - 'Part of build_plan.md Phase 4''s re-baselined yoga scope: 269 distinct named
    yogas exist in the PyJHora reference corpus; this ~20-yoga tranche covers the
    classically central set already present in this codebase''s v1 knowledge or YL-series
    scaffolding. Remaining yogas tracked in docs/yoga-compilation-backlog.md, not
    silently dropped.'
- id: YOG-007
  title: Budha-Aditya Yoga present
  category: yoga
  source_tier: 4
  source_ref: Saravali
  tradition: parasari
  conditions:
  - path: yogas.budha_aditya.present
    op: eq
    value: true
    description: Budha-Aditya Yoga present
  outputs:
  - kind: yoga_flag
    payload:
      yoga: YOG-007
      effect: Mercury and Sun conjunct within 14 degrees -- sharp intellect, eloquence,
        and analytical fame.
  confidence: medium
  status: ACTIVE
  computation_model: classical
  notes:
  - Wraps already-computed, already-tested logic in app.derived.yogas (tests/unit/test_yogas.py)
    -- this generator adds no new astrological logic, only v2 schema exposure, per
    build_plan.md §3.2.
  - 'Part of build_plan.md Phase 4''s re-baselined yoga scope: 269 distinct named
    yogas exist in the PyJHora reference corpus; this ~20-yoga tranche covers the
    classically central set already present in this codebase''s v1 knowledge or YL-series
    scaffolding. Remaining yogas tracked in docs/yoga-compilation-backlog.md, not
    silently dropped.'
- id: YOG-008
  title: Pancha Mahapurusha Yoga present (any of the 5)
  category: yoga
  source_tier: 1
  source_ref: BPHS Ch.75
  tradition: parasari
  conditions:
  - path: yogas.pancha_mahapurusha.any_present
    op: eq
    value: true
    description: Pancha Mahapurusha Yoga present (any of the 5)
  outputs:
  - kind: yoga_flag
    payload:
      yoga: YOG-008
      effect: One of Mars/Mercury/Jupiter/Venus/Saturn is in its own sign or exaltation,
        occupying a kendra from Lagna -- Maharaja-level qualities of that planet.
  confidence: high
  status: ACTIVE
  computation_model: classical
  notes:
  - Wraps already-computed, already-tested logic in app.derived.yogas (tests/unit/test_yogas.py)
    -- this generator adds no new astrological logic, only v2 schema exposure, per
    build_plan.md §3.2.
  - 'Part of build_plan.md Phase 4''s re-baselined yoga scope: 269 distinct named
    yogas exist in the PyJHora reference corpus; this ~20-yoga tranche covers the
    classically central set already present in this codebase''s v1 knowledge or YL-series
    scaffolding. Remaining yogas tracked in docs/yoga-compilation-backlog.md, not
    silently dropped.'
- id: YOG-009
  title: Neechabhanga Raja Yoga present
  category: yoga
  source_tier: 1
  source_ref: BPHS (Neechabhanga doctrine)
  tradition: parasari
  conditions:
  - path: yogas.neechabhanga_raja_yoga.present
    op: eq
    value: true
    description: Neechabhanga Raja Yoga present
  outputs:
  - kind: yoga_flag
    payload:
      yoga: YOG-009
      effect: A debilitated planet's debility is cancelled by its debilitation-sign
        lord occupying a kendra -- debility transforms into raja yoga potential.
  confidence: high
  status: ACTIVE
  computation_model: classical
  notes:
  - Wraps already-computed, already-tested logic in app.derived.yogas (tests/unit/test_yogas.py)
    -- this generator adds no new astrological logic, only v2 schema exposure, per
    build_plan.md §3.2.
  - 'Part of build_plan.md Phase 4''s re-baselined yoga scope: 269 distinct named
    yogas exist in the PyJHora reference corpus; this ~20-yoga tranche covers the
    classically central set already present in this codebase''s v1 knowledge or YL-series
    scaffolding. Remaining yogas tracked in docs/yoga-compilation-backlog.md, not
    silently dropped.'
- id: YOG-010
  title: Parivartana Yoga present
  category: yoga
  source_tier: 3
  source_ref: build_plan.md YL-001 (classification over app.derived.dispositors raw
    evidence)
  tradition: parasari
  conditions:
  - path: yogas.parivartana.present
    op: eq
    value: true
    description: Parivartana Yoga present
  outputs:
  - kind: yoga_flag
    payload:
      yoga: YOG-010
      effect: Two planets mutually occupy each other's sign of rulership -- a structural
        exchange whose auspiciousness depends on the houses involved (see evidence
        for maha/dainya-kashta/neutral classification).
  confidence: medium
  status: ACTIVE
  computation_model: classical
  notes:
  - Wraps already-computed, already-tested logic in app.derived.yogas (tests/unit/test_yogas.py)
    -- this generator adds no new astrological logic, only v2 schema exposure, per
    build_plan.md §3.2.
  - 'Part of build_plan.md Phase 4''s re-baselined yoga scope: 269 distinct named
    yogas exist in the PyJHora reference corpus; this ~20-yoga tranche covers the
    classically central set already present in this codebase''s v1 knowledge or YL-series
    scaffolding. Remaining yogas tracked in docs/yoga-compilation-backlog.md, not
    silently dropped.'
- id: YOG-011
  title: Kendradhipati Dosha present (yoga-view)
  category: yoga
  source_tier: 1
  source_ref: BPHS Ch.34 (via app.derived.functional_nature)
  tradition: parasari
  conditions:
  - path: yogas.kendradhipati_dosha.present
    op: eq
    value: true
    description: Kendradhipati Dosha present (yoga-view)
  outputs:
  - kind: yoga_flag
    payload:
      yoga: YOG-011
      effect: A natural benefic rules a kendra house alone -- loses some unconditioned
        benefic purity (does not turn malefic).
  confidence: high
  status: ACTIVE
  computation_model: classical
  notes:
  - Wraps already-computed, already-tested logic in app.derived.yogas (tests/unit/test_yogas.py)
    -- this generator adds no new astrological logic, only v2 schema exposure, per
    build_plan.md §3.2.
  - 'Part of build_plan.md Phase 4''s re-baselined yoga scope: 269 distinct named
    yogas exist in the PyJHora reference corpus; this ~20-yoga tranche covers the
    classically central set already present in this codebase''s v1 knowledge or YL-series
    scaffolding. Remaining yogas tracked in docs/yoga-compilation-backlog.md, not
    silently dropped.'
- id: YOG-012
  title: Viparita Raja Yoga present (any of Harsha/Sarala/Vimala)
  category: yoga
  source_tier: 3
  source_ref: build_plan.md YL-005 (stub_pending_translation)
  tradition: parasari
  conditions:
  - path: yogas.viparita_raja_yoga.any_present
    op: eq
    value: true
    description: Viparita Raja Yoga present (any of Harsha/Sarala/Vimala)
  outputs:
  - kind: yoga_flag
    payload:
      yoga: YOG-012
      effect: A dusthana lord (6th/8th/12th) occupies another dusthana house -- the
        mutual affliction self-cancels, and the native rises from difficulty.
  confidence: medium
  status: ACTIVE
  computation_model: classical
  notes:
  - Wraps already-computed, already-tested logic in app.derived.yogas (tests/unit/test_yogas.py)
    -- this generator adds no new astrological logic, only v2 schema exposure, per
    build_plan.md §3.2.
  - 'Part of build_plan.md Phase 4''s re-baselined yoga scope: 269 distinct named
    yogas exist in the PyJHora reference corpus; this ~20-yoga tranche covers the
    classically central set already present in this codebase''s v1 knowledge or YL-series
    scaffolding. Remaining yogas tracked in docs/yoga-compilation-backlog.md, not
    silently dropped.'
- id: YOG-013
  title: Graha Yuddha (planetary war) present
  category: yoga
  source_tier: 4
  source_ref: build_plan.md YL-006 (stub_pending_translation)
  tradition: parasari
  conditions:
  - path: yogas.graha_yuddha.present
    op: eq
    value: true
    description: Graha Yuddha (planetary war) present
  outputs:
  - kind: yoga_flag
    payload:
      yoga: YOG-013
      effect: Two planets (excluding Sun/Moon/nodes) sit within 1 degree of longitude
        -- the loser's themes and house lordships are weakened for outcome delivery.
  confidence: medium
  status: ACTIVE
  computation_model: classical
  notes:
  - Wraps already-computed, already-tested logic in app.derived.yogas (tests/unit/test_yogas.py)
    -- this generator adds no new astrological logic, only v2 schema exposure, per
    build_plan.md §3.2.
  - 'Part of build_plan.md Phase 4''s re-baselined yoga scope: 269 distinct named
    yogas exist in the PyJHora reference corpus; this ~20-yoga tranche covers the
    classically central set already present in this codebase''s v1 knowledge or YL-series
    scaffolding. Remaining yogas tracked in docs/yoga-compilation-backlog.md, not
    silently dropped.'
- id: YOG-014
  title: Kahala Yoga present
  category: yoga
  source_tier: 4
  source_ref: build_plan.md YL-004 (stub_pending_translation)
  tradition: parasari
  conditions:
  - path: yogas.kahala.present
    op: eq
    value: true
    description: Kahala Yoga present
  outputs:
  - kind: yoga_flag
    payload:
      yoga: YOG-014
      effect: 4th and 9th lords occupy mutual kendras from each other -- courageous
        wealth and authority.
  confidence: medium
  status: ACTIVE
  computation_model: classical
  notes:
  - Wraps already-computed, already-tested logic in app.derived.yogas (tests/unit/test_yogas.py)
    -- this generator adds no new astrological logic, only v2 schema exposure, per
    build_plan.md §3.2.
  - 'Part of build_plan.md Phase 4''s re-baselined yoga scope: 269 distinct named
    yogas exist in the PyJHora reference corpus; this ~20-yoga tranche covers the
    classically central set already present in this codebase''s v1 knowledge or YL-series
    scaffolding. Remaining yogas tracked in docs/yoga-compilation-backlog.md, not
    silently dropped.'
- id: YOG-015
  title: Shankha Yoga present
  category: yoga
  source_tier: 4
  source_ref: build_plan.md YL-004 (stub_pending_translation)
  tradition: parasari
  conditions:
  - path: yogas.shankha.present
    op: eq
    value: true
    description: Shankha Yoga present
  outputs:
  - kind: yoga_flag
    payload:
      yoga: YOG-015
      effect: 5th and 6th lords occupy mutual kendras from each other -- charitable
        disposition and learned prosperity.
  confidence: medium
  status: ACTIVE
  computation_model: classical
  notes:
  - Wraps already-computed, already-tested logic in app.derived.yogas (tests/unit/test_yogas.py)
    -- this generator adds no new astrological logic, only v2 schema exposure, per
    build_plan.md §3.2.
  - 'Part of build_plan.md Phase 4''s re-baselined yoga scope: 269 distinct named
    yogas exist in the PyJHora reference corpus; this ~20-yoga tranche covers the
    classically central set already present in this codebase''s v1 knowledge or YL-series
    scaffolding. Remaining yogas tracked in docs/yoga-compilation-backlog.md, not
    silently dropped.'
- id: YOG-016
  title: Lakshmi Yoga present
  category: yoga
  source_tier: 4
  source_ref: Common Parashari convention (app.knowledge.houses.RAJ_YOGA_COMBOS)
  tradition: parasari
  conditions:
  - path: yogas.lakshmi.present
    op: eq
    value: true
    description: Lakshmi Yoga present
  outputs:
  - kind: yoga_flag
    payload:
      yoga: YOG-016
      effect: 9th lord occupies a kendra in its own sign or exaltation -- immense
        wealth and fortune.
  confidence: medium
  status: ACTIVE
  computation_model: classical
  notes:
  - Wraps already-computed, already-tested logic in app.derived.yogas (tests/unit/test_yogas.py)
    -- this generator adds no new astrological logic, only v2 schema exposure, per
    build_plan.md §3.2.
  - 'Part of build_plan.md Phase 4''s re-baselined yoga scope: 269 distinct named
    yogas exist in the PyJHora reference corpus; this ~20-yoga tranche covers the
    classically central set already present in this codebase''s v1 knowledge or YL-series
    scaffolding. Remaining yogas tracked in docs/yoga-compilation-backlog.md, not
    silently dropped.'
- id: YOG-017
  title: Vasumati Yoga present
  category: yoga
  source_tier: 4
  source_ref: Common Parashari convention
  tradition: parasari
  conditions:
  - path: yogas.vasumati.present
    op: eq
    value: true
    description: Vasumati Yoga present
  outputs:
  - kind: yoga_flag
    payload:
      yoga: YOG-017
      effect: At least 2 natural benefics occupy upachaya houses (3/6/10/11) from
        Lagna -- growing prosperity over the lifetime.
  confidence: medium
  status: ACTIVE
  computation_model: classical
  notes:
  - Wraps already-computed, already-tested logic in app.derived.yogas (tests/unit/test_yogas.py)
    -- this generator adds no new astrological logic, only v2 schema exposure, per
    build_plan.md §3.2.
  - 'Part of build_plan.md Phase 4''s re-baselined yoga scope: 269 distinct named
    yogas exist in the PyJHora reference corpus; this ~20-yoga tranche covers the
    classically central set already present in this codebase''s v1 knowledge or YL-series
    scaffolding. Remaining yogas tracked in docs/yoga-compilation-backlog.md, not
    silently dropped.'
- id: YOG-018
  title: Amala Yoga present
  category: yoga
  source_tier: 4
  source_ref: Common Parashari convention
  tradition: parasari
  conditions:
  - path: yogas.amala.present
    op: eq
    value: true
    description: Amala Yoga present
  outputs:
  - kind: yoga_flag
    payload:
      yoga: YOG-018
      effect: A benefic occupies the 10th house from Lagna or from Moon -- spotless
        reputation and lasting good name.
  confidence: medium
  status: ACTIVE
  computation_model: classical
  notes:
  - Wraps already-computed, already-tested logic in app.derived.yogas (tests/unit/test_yogas.py)
    -- this generator adds no new astrological logic, only v2 schema exposure, per
    build_plan.md §3.2.
  - 'Part of build_plan.md Phase 4''s re-baselined yoga scope: 269 distinct named
    yogas exist in the PyJHora reference corpus; this ~20-yoga tranche covers the
    classically central set already present in this codebase''s v1 knowledge or YL-series
    scaffolding. Remaining yogas tracked in docs/yoga-compilation-backlog.md, not
    silently dropped.'
